name: V6 translation quality pilot

on:
  push:
    branches:
      - english-translation

permissions:
  contents: read

jobs:
  pilot:
    if: contains(github.event.head_commit.message, '[run v6 pilot]')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: english-translation
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install NLLB pilot runtime
        run: |
          python -m pip install --upgrade pip
          pip install --index-url https://download.pytorch.org/whl/cpu torch
          pip install transformers==4.56.1 sentencepiece==0.2.1 pyyaml==6.0.2
      - name: Restore German source corpus for isolated pilot
        run: |
          git checkout origin/main -- cards grammar
          python scripts/normalize_examples.py --shard 0 --shards 1
      - name: Select representative pilot files
        run: python scripts/v6_pilot.py select --output v6-pilot-paths.txt
      - name: Translate pilot with NLLB and terminology glossary
        env:
          HF_HOME: ${{ runner.temp }}/hf-cache
          TOKENIZERS_PARALLELISM: 'false'
        run: python scripts/translate_de_to_en_v6.py --paths-file v6-pilot-paths.txt --batch-size 16
      - name: Bundle pilot output
        run: python scripts/v6_pilot.py bundle --paths-file v6-pilot-paths.txt --output v6-pilot-output
      - name: Run pilot quality gate
        run: python scripts/v6_pilot.py validate --paths-file v6-pilot-paths.txt --report v6-pilot-report.txt
      - name: Upload pilot report and translated samples
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v6-pilot-output
          path: |
            v6-pilot-report.txt
            v6-pilot-paths.txt
            v6-pilot-output
          if-no-files-found: warn
